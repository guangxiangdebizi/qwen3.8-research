# 第三方实测与社区证据台账

更新时间：2026-08-04（Asia/Singapore）

## 先说边界

这不是“社区投票榜”。它把公开可访问的独立试验、开源项目、GitHub 集成问题和社区实测分层保存，用来寻找**需要复验的失败模式**，而不是用帖子数量给模型排位。

尤其要区分 `qwen3.8-max-preview` 与后续正式模型：预览服务可能持续更新。同一结论若没有模型 ID、提供方、日期、harness、推理设置和任务工件，只能算线索，不能外推为 Qwen3.8-Max 的稳定能力。

## 证据等级

| 等级 | 含义 | 可以支持的结论 |
| --- | --- | --- |
| A | 冻结输入、已知 harness/配置、明确评分规则，最好有工件或日志。 | 某一具体服务路径上的可复现实验结果；仍不等于全局排名。 |
| B | 可复现的集成缺陷或可检查的交付物，但不能完全隔离模型、工具和人工贡献。 | 兼容性或工作流风险。 |
| C | 有明确任务描述、截图或链接的用户实测，但样本很小或缺少对照。 | 生成待复验用例与失败假设。 |
| D | 主观评论、价格推算或转发。 | 发现线索；不计入能力判断。 |

## 已收集条目

| ID | 等级 | 平台与时间 | 观察 | 正确解读 |
| --- | --- | --- | --- | --- |
| E1 | A | Trilogy AI / 2026-07-19，`qwen3.8-max-preview` | 发布者以两个冻结代码库、269 个文件、OpenCode 1.17.13、60 分钟上限和 65,536 token 完成上限，对 Qwen 与 Kimi 做盲评；其报告结果为 Qwen 80、Kimi 83。Qwen 的系统边界和 replay metadata 更好，Kimi 的修订/生命周期建模更完整。 | 有方法细节，值得复跑；但每方只有一条完整服务路径，端点、缓存、推理模式和服务负载均是混杂变量，不能解释成“模型总体只差 3 分”。 |
| G1 | B | GitHub `QwenLM/qwen-code` Issue #7332 / 2026-07-20 | 在 `qwen-code` 0.20.0 中，长会话压缩等内部操作向 thinking-only preview 发送 `enable_thinking=false`，触发 400；帖中给出了模型 ID、版本、触发条件和复现步骤。Issue 后来已关闭。 | 这是 harness/API 兼容性问题，不是模型智力分数；它直接说明 agent benchmark 必须记录客户端和参数，否则“任务失败”可能错归因给模型。 |
| C1 | C | LINUX DO / 2026-07-19 | 作者描述两个实际任务：升级 Python Workout 并产出 EPUB、校对 Python Anki 卡片；报告了需求被改写、反复返工和虚构 Python 语法。 | 是两个有业务语义的负样本，适合转成私有回归测试；没有原始完整输入、对照模型和独立复现，不能据此推断整体幻觉率或训练数据污染。 |
| C2 | C | LINUX DO / 2026-07-20 | Qwen Studio 的一次 Three.js 国旗任务出现异常长的推理轨迹（帖中截图称 372 个节点）且首次没有完成目标；修改后同帖报告产物出现。 | 指向推理预算、工具适配和停止条件的风险，不是速度/能力基准。应把“是否达成验收条件、耗时、token、重试次数”同时记录。 |
| C3 | C | LINUX DO / 2026-07-20 | iOS 风格天气卡片的一次前端生成展示了正面样例；同一讨论串也有“简单问题改不好”、幻觉和路由版本差异等相互矛盾反馈。 | 说明单个漂亮 demo 的证据强度很低；短前端展示不应替代长任务、工具调用和验收测试。 |
| C4 | D | LINUX DO / 2026-07-19 | 用户根据套餐 Credits、页面 token 统计和旧型号价格推算 preview 的实际成本。 | 只保留为成本测量线索；它不是官方价目表，也不是性能证据。 |
| T1 | D | 公开 Telegram 频道 `@linuxdoit` / 采集时可见 | 频道转发了一则关于 Qoder 中 Qwen3.8-Max-Preview 实际价格的 LINUX DO 话题。 | 该频道是转发源，与原帖非独立样本；只作为发现入口，不重复计数。私有群、无法公开访问的内容不收集。 |
| U1 | C | Reddit `r/Qwen_AI` / 2026-07-29 附近 | 一位用户认为技术实现尚可，但困难任务会悄悄缩小目标或改用更容易的替代方案；其他评论有相近与相反体验。 | 可转成“需求覆盖率/禁止擅自降级”测试项；没有统一 prompt、日志或基线，不能作为横向排名。 |
| U2 | C | Reddit `r/Qwen_AI` / 2026-07-20 | 作者称借助 Qwen3.8-Max 完成 Docker 备份基础设施，并公开了代码仓库；作者也明确表示经过人工引导、审查和测试。 | 这是正向真实项目案例，可检查交付物；但无法从成品中分离模型、人工、既有依赖和后续修复的贡献，不能当作能力分数。 |

链接与原始出处见 `sources.md` 的对应证据 ID。

## 对“benchmark 很好、实际很蠢”的证据含义

现有第三方材料支持以下三个**待验证假设**：

1. **任务覆盖不等于局部正确。** 社区负样本和 Reddit 反馈都指向“给出看似合理的替代方案、却遗漏用户验收条件”。因此除了单元测试通过率，还要测需求覆盖率、禁止替代约束和人工返工率。
2. **模型、harness 和服务路径不可混为一谈。** GitHub 集成问题和 E1 都表明 thinking 参数、上下文压缩、缓存、端点、工具权限与速率限制会改变任务成败、时延和成本。
3. **预览服务是时间变量。** 2026-07 的社区体验不能直接代表之后的正式模型；必须把版本/日期写进每条结果，并在更新后重跑同一冻结任务。

它们**不能**证明训练集泄漏、benchmark 污染或“官方分数造假”。要验证污染，仍需训练数据审计、题目/解答的语义近邻检索、版本隔离，以及新鲜私有任务集上的盲测。具体执行方式见 `validation-plan.md`。

## 后续收集规则

每个新条目至少记录：原帖 URL、作者公布时间、采集时间、模型 ID/端点、客户端/harness、提示词或任务包、工具权限、推理预算、成本/时延、可检查工件和评分方式。

同一内容的 Telegram 转发、转载文章和截图只保留一个原始来源；缺少任务与配置的意见保留为 D 级，不进入任何汇总分数。这样能避免把平台热度误当成模型能力，也避免社区帖子反过来成为新的“可刷分”数据集。
