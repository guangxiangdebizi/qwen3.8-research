# Qwen3.8-Max：Benchmark 评估解读

## 先给判断

**它值得进入第一梯队候选集，但还不应被称为“已被独立验证的全局第一/第二”。**

2026-08-03 的官方发布页给出了很宽的评测表，覆盖 coding agent、general agent、通用能力、多模态、视觉 agent、文档、视频等。[O1] 这说明团队至少围绕真实工具任务和多模态工作流做了大量优化；但表中相当一部分是 Qwen 自测、内部 benchmark，且公共 benchmark 也常依赖特定 harness、工具、超时与采样配置。因此分数首先是“模型 + agent 系统 + 评测配方”的结果，而不是脱离系统后的纯模型 IQ。

## 官方表中最值得看的行

下列数值均为官方报告值，**不是本地复现结果**。[O1]

| 维度 | Qwen3.8-Max | 对照表中的高分/说明 | 初步解读 |
| --- | ---: | --- | --- |
| Terminal-Bench 2.1 | 86.6 | GPT-5.6 Sol 88.8；Fable5 84.6 | 终端工具任务很强，但不是该表最高。 |
| SWE-bench Pro | 67.7 | Fable5 80.0；GPT-5.6 Sol 64.6 | 真实仓库修复具竞争力，但和最强列仍有差距。 |
| FrontierSWE | 73.5 | Fable5 88.8 | 高难软件工程并非表内领先。 |
| PaperBench | 93.0 | GPT-5.6 Sol 90.5；Fable5 88.8 | 很亮眼；需关注论文复现流程和环境是否独立复核。 |
| GPQA Diamond | 92.6 | GPT-5.6 Sol 94.1；Fable5 92.6 | 通用高难知识/推理强，但没有表内绝对领先。 |
| HLE（无工具） | 43.6 | Fable5 53.3；GPT-5.6 Sol 47.2 | 这是对“通用、陌生难题泛化”的重要降温信号。 |
| IFBench | 82.8 | Fable5 63.5；GPT-5.6 Sol 72.7 | 指令遵循表现突出。 |
| MMMU-Pro | 82.3 | GPT-5.6 Sol 83.0；Fable5 81.2 | 多模态推理接近顶级列。 |
| OSWorld-Verified | 86.1 | Fable5 85.0；GPT-5.6 Sol 83.2 | 视觉计算机操作是官方最有说服力的强项之一。 |
| Parametric CAD Bench | 91.5 | Fable5 87.5；GPT-5.6 Sol 86.2 | 专业视觉/工程任务强，但仍需审计任务分布与验证器。 |
| OmniDocBench 1.5 | 92.1 | Gemini 3.1-Pro 90.0 | 文档理解/OCR 方向很强。 |

## 为什么不能把表直接当真实能力排名

### 1. Harness 和推理预算会显著改变结果

官方脚注中，Terminal-Bench 2.1 使用 Claude Code、`avg@10`、5 小时超时、`max_tokens=131072`；SWE-bench Pro 也使用 Claude Code，温度为 1.0、`top_p=0.95`、256K 上下文。[O1]

这测到的是：

`基础模型能力 + 工具调用格式 + 系统提示词 + agent 调度 + 运行环境 + 多次采样 + 超时预算`。

这不是缺点，反而更接近生产；但比较时必须让所有候选模型跑同一个 harness、同一套工具、相同 token/时间预算。否则“模型强”与“官方脚手架更强”无法拆开。

### 2. 表中存在内部 benchmark

官方明确标注或描述为内部评测的包括：QwenSWEBench、QwenQoderBench、QwenReactBench、QwenSVGBench、CoWorkBench、RecreationBench、QwenBlenderBench、QwenVisualOffice 等。[O1]

这些结果能说明 Qwen 针对目标场景做过优化，但不应作为跨厂商能力的独立证据。尤其是内部题库若与训练环境、奖励模型或持续评测循环有近似样本，分数可能反映的是同分布迁移，而不是 OOD 泛化。

### 3. 同一行未必是公平的 A/B

以 Terminal-Bench 为例，官方对其他模型引用“已发布结果中的最佳 harness”；FrontierSWE 则引用不同来源的公开排行榜结果。[O1] 不同模型的 agent、采样次数、工具限制、上下文窗口和重试策略不一致时，表格只能是方向性参考，不能代表严格排序。

### 4. 公开分数不等于抗污染证明

新鲜的 agent benchmark 比老的选择题更有价值，但只要题目、相近题、解法、仓库 issue 或 benchmark 讨论已进入训练/后训练数据，仍会发生直接或语义泄漏。高分同时可能包含真实能力提升和针对题型的局部泛化；详见 `training-and-contamination.md`。

## 实际使用时的预期

- **更值得试**：带工具的长程编码、视觉操作、文档/办公流、多模态输入、可执行或可自动验证的工作流。
- **不能只看宣传就替换的场景**：复杂软件工程的最终交付、一般性研究推理、高风险专业判断、长期自治 agent。
- **特别要测**：是否会长思考循环、工具调用是否会偏航、失败后能否恢复、在真实代码库和私有文档上是否产生可验证结果。这些问题不会被单个静态 benchmark 充分覆盖。

## 可复现性状态

截至 2026-08-04，我没有找到覆盖上述完整表格、采用统一公开 harness 的独立复现。发布才过去一天，合理做法是将官方表视为待验证的高质量先验，而不是最终证据。将 API 版本、地区、`reasoning_effort`、工具配置、prompt、重试策略和时间预算一并冻结后，再做自己的对照测试。

来源见 `sources.md` 的 [O1]、[O2]、[O3]。
