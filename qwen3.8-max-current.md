# 正式版 `qwen3.8-max`：版本边界与当前证据

更新时间：2026-08-04（Asia/Singapore）

## 结论先行

本资料的主对象是**正式 API 模型 `qwen3.8-max`**，不是 `qwen3.8-max-preview`。Qwen 在 2026-08-03 的发布文中称“正式发布 Qwen 3.8-Max”；当前 QwenCloud 产品页给出的模型 ID 也是 `qwen3.8-max`。[O1][O2]

因此，2026-07-19 至 2026-07-31 的 preview 社区体验、harness bug 和单次外部测试都只能解释为**历史对照或待复验用例**，不能被写成正式版的实测结论。即使帖子标题只写 “Qwen3.8-Max”，只要发布时间早于正式发布、且没有可核实的正式模型 ID，也归入 preview/未确认组。

## 当前可核实的正式版事实

| 项目 | 当前公开信息 | 证据边界 |
| --- | --- | --- |
| 正式模型 ID | `qwen3.8-max` | QwenCloud 当前页面的 API 示例直接使用此 ID。[O2] |
| 发布状态 | 2026-08-03 正式发布 | 厂商发布事实，不是第三方性能验证。[O1] |
| 架构规模 | 2.4T 总参数、95B 激活参数的 MoE | 厂商披露，尚非独立架构审计。[O1] |
| 输入/输出 | 文本、图像、视频输入；文本输出 | 产品能力声明，不代表每种输入均经过独立测评。[O2] |
| 上下文与推理预算 | 1M context；普通模式最大输入 991K、输出 131K；thinking 最大输入 983K、输出 131K、最大 reasoning 262K | 当前服务配额，可能随产品更新变化。[O2] |
| 定价 | 输入 $2/M、输出 $6/M、隐式缓存读 $0.25/M | 当前产品页价格，不与 preview 促销或第三方 Credits 推算混用。[O2] |

官方 benchmark 表中的成绩也归属于本次正式发布，但仍是厂商自报，详见 `benchmark-assessment.md`；它们不自动获得“独立测评”的地位。

## 与 preview 的强制切分

| 组别 | 可用于什么 | 不可用于什么 |
| --- | --- | --- |
| **F：正式版** | 明确写出 `qwen3.8-max`，且时间、端点与 harness 可追溯的正式版结果。 | 不能从一次服务路径推导全局排名。 |
| **P：preview/发布前** | 发现 harness、提示词、任务验收和成本测量中可能复发的问题。 | 不能成为正式版能力的正面或负面证据。 |
| **U：模型 ID 未确认** | 保存为线索，等待作者日志或复跑。 | 不能并入 F 或 P 的汇总。 |

现有 `third-party-evidence.md` 中的 E1、G1、C1-C4、T1、U1 都属于 P；U2 属于 U（发布前但原帖未公开模型 ID）。这是对既有材料的**重新标注**，不是删除。

## 本轮正式版第三方查找结果

截至本次采集，已找到的**发布后**社区材料主要是套餐、可用性和 promo 切换讨论。例如 Reddit 的 2026-08-03 讨论围绕正式版上线后的可用性与用量上限，但没有 API 请求、模型 ID、冻结任务、提示词、artifact、对照模型或可复现评分；因此它仍是 U 组线索，而非 F 组性能证据。[U3]

**本轮公开检索未找到**可以归属 `qwen3.8-max` 正式版的 A 级独立性能复现。这不等于“全网不存在”，只表示不能把 preview 的外部测试冒充为正式版结论。正式发布仅过去一天时，这个空缺是预期内的。

## 接下来应如何补正式版证据

1. 对每个外部结果先验收：`model=qwen3.8-max`、调用日期、地区/提供方、SDK 或 agent 版本、thinking 设置、工具权限和重试策略。
2. 将 preview 中 E1 的冻结仓库任务、C1 的 EPUB/知识校对任务、C2 的可视化任务原样复跑；报告正式版与 preview 的成功率、需求覆盖率、时延、token、成本及失败原因。
3. 将 GitHub/社区贴子只用来补充失败模式；只有原始任务包、日志、验证器和可复跑脚本齐全时才升级为 A 级。
4. 当 LMArena、Artificial Analysis 或公开评测框架出现明确的正式模型条目时，仍需核对版本与 provider，不能用相近名称自动匹配。

这份边界说明不会改变 `training-and-contamination.md` 的核心结论：正式版发布和正式 model ID 证明的是产品版本，不是训练数据去污染审计或真实泛化证明。
