# 验证真实泛化能力：本地评测方案

## 目标

回答的不是“Qwen3.8-Max 在公开榜上第几”，而是：**在你的数据、工具、约束和成本预算下，它是否比候选模型更稳定地完成未知任务。**

## 1. 先冻结实验条件

对每个 run 记录并固定：

- 模型 ID、API 区域、调用日期和版本快照（若供应商提供）；
- `reasoning_effort`、最大输出 token、温度、`top_p`、重试次数；
- agent harness、系统提示词、工具定义、网络权限、沙箱镜像和超时；
- 输入是否包含图片/视频，是否启用检索、代码解释器、记忆和缓存；
- 单次/多次采样的选择规则，以及总 token、工具调用、时长和费用预算。

否则比较到的可能只是不同的脚手架、思考预算或工具权限。

## 2. 用三层任务集，而不是只跑公开 benchmark

| 层级 | 建议内容 | 作用 |
| --- | --- | --- |
| 新鲜公开集 | 在模型训练截止后产生、版本可固定的题目/issue；只作外部可比较信号。 | 降低直接训练泄漏。 |
| 私有冻结集 | 已关闭的真实 bug、未公开 PR、内部文档流、生产事故复盘；答案和验证器不进入 prompt。 | 衡量你的业务泛化。 |
| 影子生产集 | 真实待办任务，由人保留最终决策；模型结果盲评或通过 CI/自动校验。 | 发现 benchmark 覆盖不到的失败模式。 |

每层按任务难度、代码库规模、是否多模态、是否需要工具、是否长程、是否需要恢复状态分层抽样。不要只挑模型擅长的任务。

## 3. 做两层去污染审计

### 字符串层

- 对训练候选数据、prompt 库、RAG 文档和测试题做 exact hash、n-gram/MinHash、代码 AST/函数相似度扫描。
- 删除测试题、标准答案、补丁、issue 标题和讨论串的近重复。

### 语义层

- 用 embedding 检索每道测试题的近邻，再用人工或盲审 LLM 判断是否同题、翻译、等价约束或相同解法模板。
- 对数学/代码任务额外生成参数替换、变量改名、语序改写、跨语言翻译版本；若分数大幅掉落，说明可能依赖表面或题型记忆。
- 检查工具/检索日志，确认模型没有访问测试答案、issue 讨论、镜像缓存或公开 leaderboard 页面。

## 4. 把“模型能力”和“agent 系统能力”分开

每个任务至少运行三种配置：

1. **模型裸测**：无工具或最小工具，单次输出；
2. **统一 harness**：所有模型使用相同的工具、系统提示词、时间/token 预算；
3. **各自最佳产品栈**：允许 Qwen/Codex/Claude Code 等使用官方推荐脚手架。

前两项回答“模型本身和公平 agent 能力”，第三项回答“你买到的产品效果”。三者不要混为一个排行榜。

## 5. 指标应以可验证交付为主

- 任务成功率；
- 单元/集成/E2E 测试通过率；
- 人工盲评质量与返工率；
- 幻觉事实率、越权工具调用率、不可恢复失败率；
- 完成时长、推理 token、工具调用数、总成本；
- 多次运行的方差，以及不同提示改写/语言下的方差；
- 长程任务的状态恢复、验证频率和最终产物可复现性。

对于代码任务，优先以隐藏测试、审查后的 diff 和回归测试为准，不要只用模型自述或 LLM-as-a-judge。

## 6. 建议的决策规则

- 只有当 Qwen3.8-Max 在私有冻结集上优于当前基线，且成本/时延在可接受范围内，才逐步扩大流量；
- 若它只在公开题或官方 harness 上领先，而在私有任务、提示改写或低工具权限下衰减明显，按“benchmark/脚手架特化”处理；
- 对高风险流程保留人工审批、回滚和可复现日志；不要把单个漂亮 demo 直接升级为自治生产权限。
