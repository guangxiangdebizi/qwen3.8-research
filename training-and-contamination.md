# 训练思路与 Benchmark 污染风险

## 已公开的 Qwen3.8-Max 训练信息

官方已明确披露的主要是 **后训练中的真实工作流 RL**，而不是完整预训练配方。[O1]

| 已披露设计 | 目标 | 对真实能力的意义 | 仍未回答的问题 |
| --- | --- | --- | --- |
| 基于 Qwen3.5 架构，2.4T 总参数、95B 激活参数的 MoE | 提升容量而保持稀疏推理成本 | 容量和稀疏路由可以支持更广任务覆盖 | 专家数、路由、预训练 token、数据质量和训练计算均未公开。 |
| 同时扩展 RL 环境和 RL 计算 | 提升 coding/work/agent 的通用工作能力 | 比“只拿大量问答 SFT”更接近实际任务。 | 环境数据源、任务新鲜度、训练/测评隔离均未公开。 |
| 环境沿 Task、Workspace、Harness 三个轴扩展 | 覆盖单任务到多日、单文件到复杂工作区、不同 agent harness | 有助于减轻单一工具/格式的过拟合。 | 轴的覆盖广度不等于与公开 benchmark 或用户任务严格独立。 |
| 通用奖励系统 | 将可执行验证、按 rubric 的文本/视觉判定、agent 检查合并为奖励 | 可把“代码是否跑通、产物是否符合要求”直接变成学习信号。 | judge 偏差、reward hacking、rubric 与测评题的重合率不明。 |
| Online data balancer | 每个 batch 平衡任务、难度、工作区、harness，降低梯度方差 | 有利于持续 RL 稳定性和横向覆盖。 | 不提供任何训练数据去污染保证。 |

官方没有公开：预训练语料和时间截止、数据许可/来源、训练样本去重策略、benchmark 黑名单、语义去污染、SFT/RL 数据比例、rollout 数、奖励模型细节、训练步数/计算量，以及完整 model card/技术报告。[O1][O2]

因此，最合理的结论是：**训练方向看起来比“刷静态题集”更偏向可执行真实环境，但公开证据不足以证明 benchmark 的独立性或真实 OOD 泛化。**

## 你的担忧需要更精确地表述

问题不是“把训练集和测试集的参数叠起来”。模型参数始终由训练样本更新；真正的问题是 **训练信息以任何形式泄漏到评测集**，使测试不再估计未知任务的泛化：

1. **精确污染**：题目、答案、代码补丁、仓库 issue 或评测页面直接出现在预训练/SFT/RL 数据中。
2. **语义污染**：训练中有同题改写、翻译、同一逻辑结构的变体，字符串不相似但解法几乎相同。
3. **评测驱动的后训练**：反复用公开榜单、相邻题库或同一风格的 verifier 调参，得到对某 benchmark 的局部适应。
4. **agent 侧泄漏**：测试中允许检索、网络、工具或已有代码库时，答案/解法可能从环境中得到；不同 harness 也会制造巨大差异。

## Qwen3.8-Max 是否给出去污染证明？

**目前没有足够公开材料支持“已严格去污染”的结论。** 官方 Qwen3.8 发布页披露了 RL 的环境和奖励设计，却没有给出 benchmark 级别的数据审计、语料截止日期、去除比例或可复核的训练数据清单。[O1]

容易混淆的一点：2025 年的 Qwen3 技术报告曾说明，对其训练数据使用 token LCS/n-gram 规则过滤潜在测试泄漏：最长公共子序列至少 13 token，且至少达到较短序列长度的 60%。[O4] 这对发现精确或近似字符串重复有帮助；但它不是 Qwen3.8-Max 的公开证明，也无法可靠发现改写、翻译、等价数学题和“同解法不同表述”的软污染。

关于后一点，2026 年的一项预印本发现，仅用 n-gram 去污染会漏掉语义重复；在受控实验中，语义重复能提高同一 benchmark 上甚至未见样本的成绩，研究者还在 Qwen3-8B-base 上观察到相同方向的效应。[R1] 这**不等于** Qwen3.8-Max 已被污染；它只是说明“有 n-gram 过滤”不足以单独证明 OOD 泛化。

## 为什么这套 RL 思路仍可能带来真实提升

相较于传统静态题库 SFT，以下信号更好：

- 代码能否编译、测试能否通过、GUI 是否真的操作成功，都能提供比语言风格偏好更硬的反馈。
- 长程环境会奖励规划、状态恢复、调试和验证，而不只是给出一次正确答案。
- 多种 workspace/harness 的混合，理论上能降低只适配单一 prompt 模板的风险。

但“奖励可验证”并不自动等于“能力泛化”：如果环境族太接近、verifier 被钻空子、训练任务与测试任务有语义重叠，模型仍然可能在 benchmark 上很强、在陌生生产约束下很笨。

## 应采用的证据标准

若要相信 Qwen3.8-Max 的 benchmark 成绩代表真实能力，至少应看到以下任一类证据：

1. 训练数据或独立第三方对每个 benchmark 的精确和语义去污染审计；
2. 训练截止日期之后创建、且未公开答案的冻结任务集；
3. 同一模型在不同 harness、不同提示风格、不同语言/改写方式下仍稳定；
4. 真实客户任务或私有代码变更，用可执行测试和人工质量复核得出的盲测结果；
5. 公开轨迹、配置和原始产物，使外部能重新计算分数而非只看汇总表。

具体落地方法见 `validation-plan.md`。

来源见 `sources.md` 的 [O1]、[O4]、[R1]。
